# ingest.py 전용 (선택) — 유튜브/음성·영상 흡수용. 코어 eb.py 는 stdlib only.
# 유튜브 자막 가져오기:
youtube-transcript-api>=0.6
# 음성/영상 파일 전사(자막 없을 때)는 무거워서 기본 제외 — 필요 시 직접 설치:
#   pip install openai-whisper   (+ 시스템에 ffmpeg 필요)
